#variable
color = 'Red'

#variable array with values
color = ['Red','Blue','Green','Orange','Pink', 'Yellow','Black','White']

v = (1)
print(v)

print(type(v))


#isValid = False

#if (isValid): # if isValid is currently true
    # do something

 #   if (not isValid): # if isValid is currently false
    #Do something else

 #       foundIndex = str.find("here") # indexnumber or -1 if doesn't exist

#if (foundIndex != -1): #if was found
    # do something
 #   userInput = "continue"

#while (userInput != "exit"):


answer = True
print(type(answer))
